---
type: analysis
title: Custom Frames HTML Artifact Writing Rule
aliases:
- Custom Frames HTML Artifact 写入规则
date_created: '2026-05-17'
source: MCP conversation capture
tags:
- custom-frames
- html-artifact
- obsidian
- second-brain
summary_en: MCP notes now keep Markdown as the durable knowledge layer while generating
  a standalone HTML artifact that Obsidian can render through Custom Frames.
summary_zh: MCP 笔记现在保留 Markdown 作为长期知识层，同时生成独立 HTML artifact，供 Obsidian 通过 Custom
  Frames 渲染。
---

# Custom Frames HTML Artifact Writing Rule
> Custom Frames HTML Artifact 写入规则

## HTML Artifact
Open the interactive HTML artifact in Obsidian through Custom Frames.
> 通过 Custom Frames 在 Obsidian 中打开交互式 HTML artifact。

```custom-frames
frame: Second Brain HTML
style: height: 760px;
urlSuffix: /20260517_mcp_custom-frames-html-artifact-writing-rule.html
```

Direct artifact URL: https://www.lucasgou.cloud/second-brain-html/20260517_mcp_custom-frames-html-artifact-writing-rule.html
> 直接访问 artifact：https://www.lucasgou.cloud/second-brain-html/20260517_mcp_custom-frames-html-artifact-writing-rule.html

## Summary
MCP notes now keep Markdown as the durable knowledge layer while generating a standalone HTML artifact that Obsidian can render through Custom Frames.
> MCP 笔记现在保留 Markdown 作为长期知识层，同时生成独立 HTML artifact，供 Obsidian 通过 Custom Frames 渲染。

## Knowledge
The note should not depend on raw inline HTML alone. A standalone HTML artifact is closer to the HTML effectiveness pattern because it can carry layout, responsive CSS, and future JavaScript without fighting Markdown sanitization. The Markdown page remains the canonical editable record, while Custom Frames turns the HTML artifact into an embedded reading surface inside Obsidian.
> 笔记不应只依赖原始内嵌 HTML。独立 HTML artifact 更接近 HTML effectiveness 的模式，因为它可以承载布局、响应式 CSS 和未来的 JavaScript，而不用和 Markdown 的安全过滤机制对抗。Markdown 页面仍然是可编辑的规范记录，Custom Frames 则把 HTML artifact 变成 Obsidian 内部的嵌入式阅读界面。

## Related
[[20260517-mcp-markdown-with-embedded-html-blocks]]
> 相关页面：[[20260517-mcp-markdown-with-embedded-html-blocks]]
